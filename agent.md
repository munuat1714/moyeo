# MVP 작업 지침

이 문서는 여행 큐레이션 MVP를 구현하는 작업자가 따라야 할 실행 규칙이다.

## 1. 작업 시작 순서

모든 작업 전에 다음 문서를 순서대로 읽는다.

1. `PRD.md`
2. `plan.md`
3. `agent.md`
4. `README.md` 및 기존 프로젝트 설정 파일

문서가 충돌하면 사용자 최신 지시, `PRD.md`, `plan.md`, `agent.md` 순서로 따른다.

## 2. 제품 목표

부산·경남권으로 1박 2일 여행을 준비하는 대학생·20대 친구 모임이 각자 취향을 입력하고, 그룹 공통 취향에 맞는 코스 3개를 비교·투표해 최종 일정을 확정할 수 있는 모바일 앱 프로토타입을 완성한다.

대표 시나리오는 부산역에서 출발하는 친구 4명의 부산 1박 2일 여행이다.

## 3. 핵심 불변 조건

다음 조건은 임의로 변경하지 않는다.

- 목적지는 사용자가 직접 정한다.
- 여행 기간은 1박 2일이다.
- 예산은 일정 생성 조건으로 받지 않는다.
- 참여자 전원이 취향 입력을 완료해야 코스를 생성할 수 있다.
- 추천 코스는 정확히 3개다.
- 세 코스는 약 60%의 일정을 공유하고 약 40%가 취향에 따라 다르다.
- 최종 코스는 참여자 투표로 결정한다.
- 투표 선택은 전원 투표 완료 전까지 다른 참여자에게 공개하지 않는다.
- 동률이면 결선투표를 진행한다.
- 음식 알레르기와 이동 제약은 선호가 아니라 필수 제약이다.
- 실제 인증, 결제, 예약 API는 구현하지 않고 현실적인 화면 흐름으로 시뮬레이션한다.

## 4. 구현 원칙

- 먼저 기존 프로젝트 구조와 변경 사항을 확인한다.
- 요구사항에 필요한 가장 작은 구현을 선택한다.
- 사용자가 실제로 누를 수 있고 상태가 변하는 프로토타입을 만든다.
- 버튼처럼 보이는 요소는 실제 동작하게 하거나 명확히 비활성 상태로 표시한다.
- 핵심 계산 로직은 UI 컴포넌트와 분리해 테스트할 수 있게 한다.
- 고정 시드 데이터라도 데이터 모델을 통해 화면에 표시한다.
- 외부 API 없이 대표 시나리오를 완전히 시연할 수 있게 한다.
- 모바일 화면을 우선하고 데스크톱에서는 모바일 앱 프레임 또는 자연스러운 반응형 레이아웃을 제공한다.
- 기존 사용자 변경사항과 관련 없는 파일은 수정하지 않는다.
- 불필요한 의존성, 추상화, 상태 관리 도구를 추가하지 않는다.

## 5. UX 원칙

- 한 화면에는 하나의 주된 행동이 보이게 한다.
- 취향 입력은 긴 폼보다 선택형 카드와 칩을 우선한다.
- 현재 단계, 완료 인원, 다음 행동을 항상 이해할 수 있게 한다.
- 코스 생성 잠금 이유와 남은 미입력자를 명확히 표시한다.
- 코스 비교 화면에서 공통 60%와 차별 40%를 시각적으로 구분한다.
- 추천 결과마다 “왜 이 코스를 추천하는지”를 보여준다.
- 다수 취향뿐 아니라 소수 취향이 어디에 반영됐는지 알 수 있게 한다.
- 예상 가격은 추천을 제한하지 않고 정보로만 표시한다.
- 터치 영역은 최소 44 × 44px을 목표로 한다.
- 색상만으로 상태를 구분하지 않는다.

## 6. 상태와 데이터 규칙

- 대표 시드 데이터는 부산역 출발, 부산 목적지 기준으로 작성한다.
- 참여자는 기본 4명으로 시연할 수 있게 한다.
- 각 참여자의 취향 응답은 독립적으로 저장한다.
- 전원 입력 여부는 파생 상태로 계산한다.
- 그룹 취향 점수는 선택 인원 기반으로 계산한다.
- 필수 제약은 점수 계산 후가 아니라 장소 후보 선정 단계에서 먼저 적용한다.
- 공통 일정과 차별 일정 여부를 데이터에 명시한다.
- 투표는 참여자 ID별 한 표로 저장한다.
- 최종 일정은 투표 결과에서 파생하거나 확정 시 명시적으로 저장한다.
- 실제 여행방 상태는 Cloudflare D1에 7일간 유지하고 참여자 인증 토큰만 `localStorage`에 저장한다.

## 7. 품질 기준

각 체크포인트 완료 전에 다음을 확인한다.

- 관련 기능을 실제 화면에서 처음부터 끝까지 실행했다.
- 추가하거나 변경한 핵심 로직에 테스트가 있다.
- 기존 테스트가 깨지지 않았다.
- TypeScript 오류가 없다.
- 브라우저 콘솔에 치명적인 오류가 없다.
- 모바일 목표 화면에서 가로 스크롤이나 잘린 주요 행동이 없다.
- 로딩, 비어 있음, 잠금, 완료 상태가 구분된다.
- 새로고침 후 시연이 중단되지 않는다.

## 8. 검증 규칙

- 설치, 테스트, 빌드, 실행 명령은 실제 프로젝트 설정을 기준으로 `README.md`에 기록한다.
- 검증이 실패하면 실패 원인을 수정하고 같은 검증을 다시 실행한다.
- 실행하지 않은 검증을 통과했다고 기록하지 않는다.
- 외부 서비스가 없어 검증할 수 없는 부분은 시뮬레이션 범위를 정확히 기록한다.
- 시각적 변경은 모바일 화면에서 직접 확인한다.
- 최종 검증에는 대표 사용자 흐름 전체가 포함되어야 한다.

## 9. 진행 기록 규칙

`plan.md`의 체크박스와 진행 기록을 실제 상태에 맞게 갱신한다.

체크포인트 보고에는 다음만 짧게 적는다.

- 완료한 기능
- 실행한 검증과 결과
- 발견한 문제와 수정 내용
- 다음 체크포인트

완료하지 않은 작업을 완료로 표시하지 않는다.

## 10. 금지 사항

- 요구사항과 무관한 대규모 리팩터링
- 실제로 동작하지 않는 가짜 버튼 방치
- 테스트를 통과시키기 위한 핵심 검증 삭제
- 민감정보나 API 키를 저장소에 추가
- 사용자 승인 없이 기존 정상 기능이나 기술 스택 교체
- 실제 예약 또는 결제가 된다고 오해할 표현
- 다수결을 이유로 필수 제약이나 소수 사용자의 안전 조건 무시

## 11. 완료 시 산출물

- 실행 가능한 모바일 우선 MVP
- 최신 상태의 `PRD.md`
- 체크포인트가 갱신된 `plan.md`
- 실행 및 검증 방법이 적힌 `README.md`
- 핵심 로직 테스트
- 부산역 → 부산 1박 2일 대표 시드 데이터
- 90초 시연이 가능한 초기화된 데모 흐름
