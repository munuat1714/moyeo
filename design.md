# 모행 디자인 가이드

- 문서 버전: v1.0
- 기준 플랫폼: 모바일 앱 프로토타입
- 기준 화면: 390 × 844px
- 디자인 방향: 토스에서 느껴지는 명확성, 속도감, 신뢰감을 여행 서비스에 맞게 재해석

## 1. 디자인 목표

사용자가 설명을 읽지 않아도 현재 상황과 다음 행동을 바로 이해하는 인터페이스를 만든다.

> 친구들의 복잡한 취향 조율은 앱이 처리하고, 사용자는 중요한 선택에만 집중한다.

토스의 상표, 로고, 고유 화면을 복제하지 않는다. 다음과 같은 상위 경험 원칙만 참고한다.

- 큰 제목과 짧은 설명
- 한 화면에 하나의 핵심 행동
- 중요한 숫자와 상태를 먼저 보여주는 구조
- 충분한 여백과 단순한 카드
- 회색 기반 화면에 선명한 단일 강조색
- 어렵거나 딱딱한 표현보다 자연스러운 대화체
- 상태 변화에 즉각적으로 반응하는 인터랙션

## 2. 핵심 경험 원칙

### 2.1 한 화면, 한 가지 결정

각 화면에는 가장 중요한 CTA를 하나만 둔다.

- 여행 생성 화면: `여행방 만들기`
- 참여 현황 화면: `우리 취향 분석하기`
- 코스 추천 화면: `친구들과 투표하기`
- 투표 결과 화면: `1위 코스로 확정하기`

보조 행동은 텍스트 버튼이나 낮은 대비의 보조 버튼으로 표현한다.

### 2.2 결론부터 보여주기

사용자가 가장 궁금해하는 정보를 화면 상단에 먼저 보여준다.

- 참여 현황: `3/4명 완료`
- 그룹 분석: `맛집 4명 선택`
- 코스 추천: `취향 일치 92%`
- 투표 결과: `3표로 1위`
- 예약 현황: `1/3 완료`

상세한 이유와 설명은 결과 아래에서 확인할 수 있게 한다.

### 2.3 선택 부담 줄이기

- 취향은 최대 3개로 제한한다.
- 추천 코스는 정확히 3개만 제공한다.
- 공통 일정과 다른 일정을 색상 및 라벨로 구분한다.
- 코스 비교 항목은 취향 일치율, 예상 가격, 이동시간으로 통일한다.
- 기본 추천 코스를 명확하게 표시하되 선택을 강요하지 않는다.

### 2.4 신뢰할 수 있는 추천

추천 결과마다 이유를 함께 보여준다.

- `4명 모두 맛집을 선택했어요`
- `유나님의 견과류 알레르기를 반영했어요`
- `현우님의 액티비티 취향은 특별 일정에 담았어요`
- `세 코스 중 이동시간이 가장 짧아요`

AI나 알고리즘을 강조하기보다 사용자의 응답이 어떻게 반영됐는지를 설명한다.

## 3. 시각 언어

### 3.1 색상

중립적인 회색 배경과 흰색 카드를 기본으로 사용하고, 핵심 행동에는 블루를 사용한다.

| 역할 | 토큰 | 색상 |
|---|---|---|
| 주요 행동 | `color-primary` | `#3182F6` |
| 주요 행동 눌림 | `color-primary-pressed` | `#1B64DA` |
| 주요 행동 배경 | `color-primary-soft` | `#E8F3FF` |
| 기본 텍스트 | `color-text-primary` | `#191F28` |
| 보조 텍스트 | `color-text-secondary` | `#4E5968` |
| 약한 텍스트 | `color-text-tertiary` | `#8B95A1` |
| 앱 배경 | `color-background` | `#F2F4F6` |
| 카드 | `color-surface` | `#FFFFFF` |
| 구분선 | `color-border` | `#E5E8EB` |
| 성공 | `color-success` | `#00A878` |
| 주의 | `color-warning` | `#F59F00` |
| 오류 | `color-danger` | `#F04452` |

사용 규칙:

- 블루는 핵심 CTA, 선택 상태, 핵심 수치에 집중적으로 사용한다.
- 한 화면에서 강한 강조색은 블루 한 가지를 원칙으로 한다.
- 성공, 경고, 오류 색상은 해당 상태에서만 사용한다.
- 장식 목적의 그라데이션은 사용하지 않는다.
- 텍스트 대비는 WCAG AA 이상을 목표로 한다.

### 3.2 타이포그래피

권장 글꼴:

```css
font-family:
  Pretendard,
  -apple-system,
  BlinkMacSystemFont,
  "Apple SD Gothic Neo",
  "Noto Sans KR",
  "Malgun Gothic",
  sans-serif;
```

| 스타일 | 크기/행간 | 굵기 | 용도 |
|---|---:|---:|---|
| Display | 32/42 | 700 | 홈의 핵심 메시지 |
| Title 1 | 26/36 | 700 | 화면 제목 |
| Title 2 | 22/31 | 700 | 결과 및 카드 제목 |
| Heading | 18/27 | 700 | 섹션 제목 |
| Body | 16/24 | 400 | 기본 본문 |
| Body small | 14/21 | 400 | 보조 설명 |
| Caption | 12/18 | 500 | 라벨, 메타 정보 |
| Numeric | 28/34 | 700 | 완료율, 일치율, 가격 |

문장 규칙:

- 제목은 최대 두 줄로 제한한다.
- 본문은 한 문장에 하나의 내용만 담는다.
- 숫자와 단위 사이에는 불필요한 줄바꿈이 생기지 않게 한다.
- 영어 대문자보다 자연스러운 한국어를 사용한다.

### 3.3 간격과 레이아웃

- 화면 좌우 여백: 20px
- 섹션 사이 간격: 32px
- 제목과 설명 사이: 8px
- 카드 내부 여백: 20px
- 카드 사이 간격: 12px
- 기본 8px 그리드 사용
- 하단 CTA 영역: 안전 영역을 포함해 최소 88px 확보
- 콘텐츠 최대 너비: 모바일 430px

### 3.4 모서리와 그림자

- 기본 카드 반경: 20px
- 작은 카드·입력 반경: 16px
- 버튼 반경: 16px
- 칩 반경: 999px
- 테두리: `1px solid #E5E8EB`
- 그림자는 핵심 플로팅 CTA 또는 모달에만 사용한다.

기본 카드에는 진한 그림자 대신 배경과 테두리 차이로 위계를 만든다.

### 3.5 아이콘

- 선형 아이콘을 사용한다.
- 기본 크기: 20px
- 작은 라벨: 16px
- 터치 영역은 아이콘 크기와 관계없이 최소 44×44px
- 같은 행동에 항상 같은 아이콘을 사용한다.
- 의미 없는 장식 아이콘과 이모지는 줄인다.

## 4. 컴포넌트

### 4.1 Primary Button

- 높이: 56px
- 배경: `#3182F6`
- 텍스트: 흰색, 16px, 700
- 화면 하단에 고정할 수 있다.
- 비활성 상태는 회색 배경과 낮은 대비를 사용한다.
- 로딩 중에는 문구를 유지하고 작은 진행 표시를 추가한다.

### 4.2 Secondary Button

- 높이: 52px
- 배경: `#E8F3FF`
- 텍스트: `#1B64DA`
- 핵심 흐름을 대신하지 않는 보조 행동에 사용한다.

### 4.3 Text Button

- 높이: 최소 44px
- 배경과 테두리 없음
- 보조 텍스트 색상 또는 블루 사용
- `초대 링크 복사`, `처음부터 다시 하기` 등에 사용한다.

### 4.4 Card

- 흰색 배경
- 20px 내부 여백
- 20px 모서리
- 필요한 경우에만 연한 테두리
- 카드 하나에는 하나의 정보 묶음만 담는다.

### 4.5 Choice Chip

- 높이: 44px 이상
- 미선택: 흰색 또는 연한 회색
- 선택: 연한 블루 배경, 블루 텍스트와 체크 아이콘
- 색상 외에도 체크 아이콘으로 선택 상태를 표현한다.

### 4.6 Progress

- 단계 번호보다 현재 해야 할 일을 먼저 보여준다.
- `친구 취향 입력`과 같은 단계명 옆에 작은 진행 막대를 배치한다.
- 참여 현황은 `3/4명 완료`처럼 숫자로 함께 제공한다.

### 4.7 Bottom CTA

- 화면 하단에 고정한다.
- 좌우 20px 여백
- 기기 안전 영역을 포함한다.
- 본문 마지막 내용이 CTA 뒤에 가려지지 않게 하단 패딩을 둔다.

### 4.8 Bottom Sheet

장소 상세, 예약 연결, 코스 설명처럼 현재 맥락을 유지해야 하는 정보에 사용한다.

- 상단 핸들 제공
- 닫기 버튼 제공
- 제목, 핵심 정보, 행동 순서로 구성
- 배경 스크롤 잠금

## 5. 화면별 가이드

### 5.1 홈

- 핵심 문장: `친구 취향을 모으면 여행이 완성돼요`
- 설명은 두 줄 이내
- 대표 행동 `새 여행 만들기`를 하단에 배치
- 복잡한 지도 일러스트보다 간결한 여행 요약 카드 사용
- 최근 여행이 있으면 핵심 CTA 아래에 한 개만 노출

### 5.2 여행 만들기

- 한 화면에 모든 필드를 넣지 않는다.
- 목적지 → 날짜·인원 → 이동수단 순으로 단계화한다.
- 사용자가 입력한 값을 다음 화면의 자연스러운 문장으로 요약한다.
- 입력 오류는 해당 필드 바로 아래에서 설명한다.

### 5.3 친구 참여 현황

- `3/4명 완료`를 가장 크게 보여준다.
- 완료와 미완료 참여자를 즉시 구분한다.
- 코스 생성이 잠긴 이유를 버튼 문구로 설명한다.
- 미입력 친구에게 다시 요청하는 행동은 보조 버튼으로 둔다.

### 5.4 그룹 취향 분석

- 1순위 공통 취향을 큰 결과 카드로 보여준다.
- 나머지 취향은 간결한 순위 또는 막대로 표현한다.
- 알레르기 등 필수 조건은 별도의 안전 영역으로 분리한다.
- 분석 과정 대신 결과와 추천에 미친 영향을 설명한다.

### 5.5 코스 비교

상단 비교 요약:

- 취향 일치율
- 1인 예상 가격
- 총 이동시간
- 코스 분위기

공통 일정에는 `함께 가요`, 다른 일정에는 `이 코스만` 라벨을 붙인다. 사용자가 좌우로 넘기지 않아도 세 코스의 핵심 차이를 파악할 수 있는 비교 요약을 우선한다.

### 5.6 투표

- 현재 투표 중인 친구를 명확히 표시한다.
- 코스 이름, 일치율, 핵심 취향만 보여준다.
- 투표 완료 전에는 중간 결과를 노출하지 않는다.
- 전원 완료 후 결과를 숫자와 막대로 보여준다.
- 동률이면 결선투표 이유와 남은 후보를 간단히 설명한다.

### 5.7 최종 일정

- 상단에 `투표로 확정된 여행` 상태를 표시한다.
- 일정, 동선, 예약을 3개의 탭으로 구분한다.
- 시간표는 시간 → 장소 → 이유 → 가격 순서로 읽힌다.
- 예약이 필요한 항목만 별도 목록으로 모은다.

## 6. 모션과 피드백

- 일반 상태 전환: 150~200ms
- 화면 진입: 200~250ms
- 바텀시트: 250~300ms
- 진행률 변화는 너비 애니메이션으로 보여준다.
- 투표 완료와 최종 확정에는 짧은 성공 피드백을 제공한다.
- 과도한 바운스, 긴 로딩 연출, 장식용 패럴랙스는 사용하지 않는다.
- `prefers-reduced-motion` 설정을 존중한다.

## 7. 카피 톤

친구에게 안내하듯 짧고 구체적으로 말한다.

권장:

- `어디로 떠날까요?`
- `모두 입력하면 코스를 만들어요`
- `우리 취향이 모였어요`
- `3명이 감성 카페를 골랐어요`
- `공동 1위라 한 번 더 투표해요`

피해야 할 표현:

- `사용자 선호도 분석이 완료되었습니다`
- `AI 알고리즘 기반의 최적 경로`
- `해당 기능을 이용할 수 없습니다`
- `정상적으로 처리되었습니다`

오류 메시지는 문제와 해결 방법을 함께 전달한다.

- `목적지를 입력해 주세요`
- `아직 현우님이 취향을 입력하지 않았어요`
- `연결이 끊겼어요. 다시 시도해 주세요`

## 8. 접근성

- 본문 텍스트는 최소 14px을 권장한다.
- 터치 영역은 최소 44×44px로 만든다.
- 모든 입력에는 화면에서 확인 가능한 라벨을 둔다.
- 아이콘 버튼에는 접근 가능한 이름을 제공한다.
- 선택, 완료, 오류를 색상만으로 구분하지 않는다.
- 키보드 포커스 스타일을 제거하지 않는다.
- 모달과 바텀시트가 열리면 포커스를 내부로 이동한다.
- 이미지와 지도형 그래픽에는 대체 설명을 제공한다.

## 9. 디자인 적용 우선순위

### P0

- 주요 강조색을 블루로 통일
- 화면 제목과 핵심 수치의 타이포그래피 강화
- 카드 그림자 축소 및 회색 배경 적용
- Primary CTA 스타일 통일
- 선택 상태를 연한 블루와 체크 아이콘으로 통일

### P1

- 여행 생성 화면 단계 분리
- 코스 3개 비교 요약 개선
- 장소 상세와 예약 연결을 바텀시트로 변경
- 성공, 잠금, 오류 피드백 개선

### P2

- 부드러운 화면 전환
- 투표 완료 마이크로 인터랙션
- 로딩 스켈레톤
- 다크 모드 검토

## 10. 디자인 검수 체크리스트

- [ ] 화면의 가장 중요한 정보가 3초 안에 보인다.
- [ ] 핵심 CTA가 한 개만 강하게 강조된다.
- [ ] 제목과 설명이 짧고 자연스럽다.
- [ ] 블루 외의 강조색이 과도하게 사용되지 않는다.
- [ ] 카드가 불필요하게 중첩되지 않는다.
- [ ] 선택과 완료 상태를 색상 외의 방법으로도 알 수 있다.
- [ ] 모든 터치 영역이 44×44px 이상이다.
- [ ] 320px 화면에서 가로 스크롤이 발생하지 않는다.
- [ ] 하단 CTA가 콘텐츠를 가리지 않는다.
- [ ] 로딩, 잠금, 비어 있음, 오류 상태가 정의되어 있다.
- [ ] 실제 결제나 예약이 된다고 오해할 표현이 없다.
